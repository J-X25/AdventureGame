// Smart counting with a for loop

for (let count = 1; count <= 100; count++) {
    if (count === 50) {
        console.log(count + " - Halfway!");
    } else {
        console.log(count);
    }
}
console.log("Counting complete!");