// Keep adding numbers until we reach 10

let sum = 0;
let target = 20;
while (sum < target) {
sum = sum + 2;
console.log("Sum is now: " + sum);
}
console.log("Reached the target!");

// Reaching a target

