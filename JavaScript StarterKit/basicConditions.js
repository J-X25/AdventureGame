// Condition: is the user logged in?
let userLoggedIn = true;

// Simple "if" statement
if (userLoggedIn) {
    console.log("Welcome back! Proceed to checkout.");
}

// Check the "not" if condition
if (!userLoggedIn) {
    console.log("Please log in to continue to checkout.");
}

